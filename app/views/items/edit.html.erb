<%= error_messages_for :item %>

<% form_for :item, :url => item_path(@item), :html => { :method => :put, :class => "cmxform" } do |f| %>

	<%= render :partial => 'form', :locals => {:f => f} %>

	<p>
		<%= submit_tag 'Save changes', :class => 'button positive accept' %>
	</p>
<% end %>