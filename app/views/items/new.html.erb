
<% form_for(:item, :url => items_path, :html => { :multipart => true, :class => "cmxform" }) do |f| %>

  <%= error_messages_for :item %>

	<%= render :partial => 'form', :locals => {:f => f} %>

	<p>
		<%= submit_tag "Create a new item", :class => 'button positive add' %>
	</p>
<% end %>