<%= error_messages_for :child %>

<% form_for(:child, :url => item_screenshot_path(@item, @child), :html => { :method => :put, :class => 'cmxform' }) do |f| %>

  <%= render :partial => 'form', :locals => {:f => f} %>

  <p>
	  <%= submit_tag "Update" %>
  </p>
<% end %>