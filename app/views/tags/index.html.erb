
<h1><%= link_to pagetitle("Popular Tags"), tags_path %></h1>

<div class="full">

  <% for tag in @tags %>
  	<span style="font-size: <%= ( tag.popularity.to_i + 60)/2  %>px;">
  	  <%= link_to clean(tag.name), tag_url(tag) %>
  	</span>
  <% end %>

</div>