<%= error_messages_for :child %>

<% form_for(:child, :url => item_version_path(@item, @child), :html => { :class => 'cmxform', :method => :put }) do |f| %>
	<%= render :partial => 'form', :locals => {:f => f} %>

	<p>
		<%= submit_tag "Update" %>
	</p>
<% end %>