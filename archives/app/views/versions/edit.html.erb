<%= error_messages_for :child %>

<% form_for(:child, :url => version_path(@item, @child), :html => { :method => :put }) do |f| %>
	<%= render :partial => 'form', :locals => {:f => f} %>

	<p>
		<%= submit_tag "Update" %> or <%= link_to 'Cancel', item_path(@item) %>
	</p>
<% end %>